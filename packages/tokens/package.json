{
  "name": "@wmde/wikit-tokens",
  "private": true,
  "version": "0.0.0",
  "description": "Design tokens for wikit in different formats",
  "author": "The Wikidata team",
  "homepage": "https://github.com/wmde/wikit#readme",
  "license": "BSD-3-Clause",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wmde/wikit.git"
  },
  "scripts": {
    "test": "echo \"Tests of tokens\"",
    "postinstall": "echo \"installed tokens\"",
    "prepare": "node build/wikimedia-ui-base",
    "build": "npm run clean && npm run build:tokens",
    "prepublish": "npm run build",
    "clean": "rm -rf ./dist ./docs",
    "build:tokens": "style-dictionary build -c ./.style-dictionary/config.json"
  },
  "bugs": {
    "url": "https://github.com/wmde/wikit/issues"
  },
  "devDependencies": {
    "less-to-json": "^1.2.0",
    "style-dictionary": "^2.10.0",
    "wikimedia-ui-base": "^0.15.0"
  }
}
