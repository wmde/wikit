import { Meta } from '@storybook/addon-docs/blocks';
import { flattenTokenTree } from './lib/flattenTokenTree';
import { opacity } from '@wmde/wikit-tokens';

<Meta title="Design Tokens|Opacity" />

## Opacity

<table name="opacity" style={{ width: '100%' }}>
    <tr>
        <th>Name</th>
        <th>Source</th>
        <th>Value</th>
    </tr>
{
    flattenTokenTree( opacity ).map( ( opacity, index ) => (
        <tr key={index}>
            <td>{opacity.name}</td>
            <td>{opacity.referencedTokens}</td>
            <td>{opacity.value}</td>
        </tr>
    ) )
}
</table >