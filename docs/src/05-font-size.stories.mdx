import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import { flattenTokenTree } from './lib/flattenTokenTree';
import { dimension } from '@wmde/wikit-tokens';

<Meta title="Design Tokens|Font size" />

## Font size

<table name="font-size" style={{ width: '100%' }}>
    <thead>
        <tr>
            <th>Name</th>
            <th>Source</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
{
    flattenTokenTree( dimension.font.size ).map( ( { name, referencedTokens, value }, index ) => (
        <tr key={index}>
            <td>{name}</td>
            <td>{referencedTokens || '-'}</td>
            <td>
                <Typeset
                    fontSizes={[ value ]}
                    sampleText="The quick brown fox jumps over the lazy dog"
                />
            </td>
        </tr>
    ) )
}
    </tbody>
</table >
