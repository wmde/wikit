import { Meta, Typeset } from '@storybook/addon-docs/blocks';
import { flattenTokenTree } from './lib/flattenTokenTree';
import * as tokens from '@wmde/wikit-tokens';

<Meta title="Design Tokens|z-index" />

## z-index

<table name="z-index" style={{ width: '100%' }}>
    <thead>
        <tr>
            <th>Name</th>
            <th>Source</th>
            <th>Value</th>
        </tr>
    </thead>
    <tbody>
{
    flattenTokenTree( tokens[ 'z-index' ] ).map( ( { name, referencedTokens, value } ) => (
        <tr key={ name }>
            <td>{ name }</td>
            <td>{ referencedTokens || '-' }</td>
            <td>
                <pre>{ value }</pre>
            </td>
        </tr>
    ) )
}
    </tbody>
</table >
