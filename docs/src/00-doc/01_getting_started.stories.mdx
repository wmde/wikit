import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Documentation/Getting started" />

# WiKit

This documents the work on the WiKit product, our Wikidata / Wikibase Design System.

The documentation is organized into these categories:

* How to modify existing components - if you want to change released components in this Storybook
* How to create reusable components - if you want to create a new system-compliant component
* Decisions and ADRs - if you want to understand technical decisions or why the process was defined as described

## Using vue-components

**The library assumes the consuming application uses Vue.js v2.6+.**

#### 1. Install via npm

`npm install @wmde/wikit-vue-components`

#### 2. Import styles

In your style entry point(s), e.g. `_main.scss`, add

`@import '~@wmde/wikit-vue-components/dist/wikit-vue-components.css';`

or in your entry component, e.g `App.vue`, add

`import '@wmde/wikit-vue-components/dist/wikit-vue-components.css';`

#### 3. Import a component

`import { Button } from '@wmde/wikit-vue-components';`

## Using tokens

The Design System tokens are packaged separately from the `vue-components`.
You can use them in any type of front end.

#### 1. Install via npm

`npm install @wmde/wikit-tokens`

#### 2. Import

The tokens are served in three different formats:

`@import '~@wmde/wikit-tokens/dist/_variables_.less';`

`@import '~@wmde/wikit-tokens/dist/_variables_.scss';`

`@import '~@wmde/wikit-tokens/dist/_variables_.css';`
