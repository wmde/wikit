{
  "name": "wikit",
  "private": true,
  "license": "BSD-3-Clause",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wmde/wikit.git"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "lerna run test",
    "storybook": "lerna run storybook --parallel --stream",
    "build:storybook": "lerna run build --scope @wmde/wikit-docs && lerna run build:storybook --scope @wmde/wikit-vue-components",
    "chromatic": "lerna run chromatic --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
